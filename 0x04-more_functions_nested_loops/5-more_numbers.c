#include "main.h"

/**
 * more_numbers - print them
 */
void more_numbers(void)
{
	int i, j, k;

	for (i = 1; i = 10; i++)
	{
		for (j = 0; j <= 14; j++)
		{
			j = k;
			if (k > 9)
			{
				_putchar(1 + 48);
				j = k % 10;
			}
			_putchar(j + 48);
		}
		_putchar('\n');
	}
}
